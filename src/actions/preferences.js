import * as types from '../actionTypes/preferences';

// eslint-disable-next-line import/prefer-default-export
export const showBanner = () => ({ type: types.SHOW_BANNER });
export const hideBanner = () => ({ type: types.HIDE_BANNER });
